apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: example-project-quickstart
  namespace: argocd
spec:
  project: default
  # Manual sync: deploys only when you choose to sync it from the UI.
  source:
    repoURL: https://github.com/dhpup/akuity-examples.git
    targetRevision: main
    path: project-quickstart/argocd
    directory:
      recurse: true
  destination:
    name: in-cluster
    namespace: argocd
